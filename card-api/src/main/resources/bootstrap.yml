# ==============================================================
# CONFIGURAÇÃO DE BOOTSTRAP - LIDO ANTES DE application.yml
# ==============================================================
spring:
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
      fail-fast: true
      name: hyperativa-services
      label: main
