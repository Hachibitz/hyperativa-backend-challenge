server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: api
  profiles:
    active: local

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html